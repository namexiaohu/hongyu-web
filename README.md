# hongyu-web

HONGYU Medical 公司官网（Next.js App Router）。运行于 **http://localhost:5000**。

本期为静态页面，不调用 `hongyu-admin` Front API。

```bash
pnpm install
cp .env.example .env
pnpm dev   # http://localhost:5000
```
